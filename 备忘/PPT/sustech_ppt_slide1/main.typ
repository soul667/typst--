#import "./lib.typ": slides, two-col, three-col
#set text(font: ("Times New Roman", "Noto Sans SC"), size: 12pt)

// 导入常用包（按需使用）
// #import "@preview/cetz:0.4.2": canvas
// #import "@preview/fletcher:0.5.8" as fletcher: edge, node
// #import "@preview/frame-it:1.2.0": *

// ════════════════════════════════════════════════════════════
// 南方科技大学主题色（可选自定义）
// ════════════════════════════════════════════════════════════
#let sustech-orange = color.rgb(237, 108, 0)

// ════════════════════════════════════════════════════════════
// 模板配置
// ════════════════════════════════════════════════════════════
#show: slides.with(
  title: "演示文稿标题",
  subtitle: "副标题/说明",
  date: "2025年",
  authors: ("作者姓名",),

  // 页面设置
  ratio: 16 / 9,        // 16:9 或 4/3
  layout: "medium",     // small/medium/large

  // 可选配置
  toc: true,            // 是否显示目录
  footer: true,         // 是否显示页脚
  count: "dot",         // 页面指示: "dot"/"number"/"dot-section"/none
  theme: "normal",      // "normal"/"full"

  // 可选：自定义颜色（默认使用南科大橙色）
  // title-color: color.rgb(237, 108, 0),

  // 可选：自定义Logo和章节图片
  // logo: "img/SUSTech LOGO CN.svg",
  // section-image: "img/页面 1.png",
)

// ════════════════════════════════════════════════════════════
// Section 1: 引言
// ════════════════════════════════════════════════════════════
= 引言

== 研究背景

这里是研究背景介绍。

- 要点一
- 要点二
- 要点三

== 研究目标

#align(center)[
  #block(
    width: 80%,
    fill: sustech-orange.lighten(90%),
    inset: 1em,
    radius: 4pt,
  )[
    *主要目标*
    
    清晰、简洁地表达研究目标
  ]
]

// ════════════════════════════════════════════════════════════
// Section 2: 方法
// ════════════════════════════════════════════════════════════
= 方法

== 双栏布局示例

#two-col[
  *左侧内容*
  
  使用 `#two-col` 函数可以创建双栏布局。
  
  + 编号列表项
  + 另一个列表项
][
  *右侧内容*
  
  可以自定义栏宽比例：
  
  `#two-col(ratio: (3fr, 2fr))[...][...]`
]

== 表格示例

#table(
  columns: (1fr, 1fr, 1fr),
  align: (left, center, center),
  [*方法*], [*准确率*], [*速度*],
  [方法 A], [85%], [快],
  [方法 B], [92%], [中等],
  [我们的方法], [*96%*], [*快*],
)

// ════════════════════════════════════════════════════════════
// Section 3: 结果
// ════════════════════════════════════════════════════════════
= 结果

== 关键发现

#three-col[
  *发现 1*
  
  简要描述
][
  *发现 2*
  
  简要描述
][
  *发现 3*
  
  简要描述
]

== 结论

// ════════════════════════════════════════════════════════════
// 参考文献（可选）
// ════════════════════════════════════════════════════════════
// = References
// == Bibliography
// #bibliography("refs.bib")
